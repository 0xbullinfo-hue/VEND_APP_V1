/**
 * Shim for iceberg-js — required by @supabase/storage-js@2.106.1
 * but not actually used by this app. Provides an empty export so
 * Metro can resolve the module without error.
 */
module.exports = {};
